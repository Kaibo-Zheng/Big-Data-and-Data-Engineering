# Data Files

Place the official CCKS2026 Task 6 data files in this directory before running
the training or inference scripts:

```text
train.jsonl
test_a_release.jsonl
```

The JSONL files are intentionally not tracked in Git because the release files
are large. The project code expects these filenames by default.
